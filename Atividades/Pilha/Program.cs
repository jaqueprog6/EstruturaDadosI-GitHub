using StackClass;

Stack pilha = new Stack(); // Stack = método construtor (tem que ter o mesmo nome da classe)

pilha.Push(10);
pilha.Push(9);
pilha.Push(100);
pilha.Push(50);

pilha.PrintStack();